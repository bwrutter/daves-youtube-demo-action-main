const a = 10;
const b = 20;

function soma(a, b) {
    const resultado = a + b;
    return `Deu certo a soma viu ${resultado}`;
}

console.log(soma(a, b));

function subtracao(a, b) {
    const resultado = a - b;
    return `Deu certo a subtração viu ${resultado}`;
}

console.log(subtracao(a, b));

function multiplicacao(a, b) {
    const resultado = a * b;
    return `Deu certo a multiplicação viu ${resultado}`;
}