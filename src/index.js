const a = 10;
const b = 20;

function soma(a, b) {
    const resultado = a + b;
    return `Deu certo a soma viu!${resultado}`;
}

function subtracao(a, b) {
    const resultado = a;
    return `Deu certo a subtração viu!!! ${resultado}`;
}

function multiplicacao(a, b) {
    const resultado = a * b;
    return `Deu certo a multiplicação viu!!! ${resultado + resultado}`;
}